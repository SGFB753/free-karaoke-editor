# Базовая работа: ничего из этого не обязательно.
# Нужен только Python 3.8+ и ffmpeg в системе.

# --- точная разметка по словам (forced alignment моделью Whisper) ---
stable-ts>=2.17

# --- минусовка: отделение вокала от инструментала ---
demucs>=4.0
# torchaudio 2.x сам писать WAV не умеет, а demucs эту зависимость не тянет
soundfile>=0.12

# --- ускоряет разбор громкости (не обязательно) ---
numpy>=1.21

# --- рендер ролика в MP4 (tools/video.py) ---
pillow>=9.0

# --- если нет прав ставить ffmpeg в систему, раскомментируйте ---
# imageio-ffmpeg
