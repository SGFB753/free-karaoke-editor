# The basics need none of this: Python 3.8+ and ffmpeg in the system are enough.

# --- word-by-word timing (forced alignment with the Whisper model) ---
stable-ts>=2.17

# --- the instrumental: separating the vocal from the backing ---
demucs>=4.0
# torchaudio 2.x cannot write WAV itself, and demucs does not pull this in
soundfile>=0.12

# --- speeds up the loudness analysis (optional) ---
numpy>=1.21

# --- a song from a link: the studio takes the sound out of a video link ---
yt-dlp>=2024.1.1

# --- rendering the MP4 video (tools/video.py) ---
pillow>=9.0

# --- uncomment if you cannot install ffmpeg system-wide ---
# imageio-ffmpeg
